package com.backend.uberclone.repository;

import com.backend.uberclone.model.FavouriteRoute;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FavoriteRouteRepository extends JpaRepository<FavouriteRoute,Integer> {
}
